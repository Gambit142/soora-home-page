import logo from '../assets/images/logo.png';

const Navigation = () => (
  <nav>
    <div>
      <img src={logo} alt="soora-logo" />
    </div>
    <div>

    </div>
  </nav>
);

export default Navigation;